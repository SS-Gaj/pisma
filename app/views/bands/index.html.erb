<% provide(:title, 'Новости') %>
  <h1>Новости</h1>
<div class="row">
	<div class="span8">
		<%= will_paginate %>
			<ol class="microposts">
      	<%= render @bands %>
			</ol>

		<%= will_paginate %>
	</div>
</div>
