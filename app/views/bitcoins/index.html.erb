<% provide(:title, 'Биткоин') %>
  <h1>Биткоин</h1>
<div class="row">
	<div class="span8">
		<%= will_paginate %>
			<ol class="microposts">
      	<%= render @bitcoins %>
			</ol>

		<%= will_paginate %>
	</div>
</div>
